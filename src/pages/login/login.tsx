/** @format */

import CreateForm from '../../components/createForm/createForm';

const Login = () => {
	return (
		
		<CreateForm
			children={'Login'}
			pageName={'Login'}
			description={'Please Login '}
		/>
	);
};

export default Login;

